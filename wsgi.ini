[uwsgi]
socket = /run/vlado/wsgi.socket
pidfile = /run/vlado/wsgi.pid
stats = /run/vlado/wsgi_stats.sock

plugins = python3
venv = /home/asd/.virtualenvs/vlado.lan/

wsgi-file = wsgi.py
callable = app
processes = 5

master = true

