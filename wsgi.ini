[uwsgi]
socket = /run/vlado/wsgi.socket
pidfile = /run/vlado/wsgi.pid
stats = /run/vlado/wsgi_stats.sock

plugins = python3
venv = /home/zloy/.virtualenvs/vlado/

wsgi-file = wsgi.py
callable = app
processes = 5

master = true

